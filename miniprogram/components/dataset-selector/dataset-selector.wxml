<view class="dataset-selector">
  <picker bindchange="bindDatasetChange" value="{{datasetIndex}}" range="{{datasetList}}">
    <view class="picker">
       {{datasetList[datasetIndex]}}
    </view>
  </picker>
</view>